Author-exported UI mocks land here as PNG (see CLAUDE.md "UI mock gate" for
expected filenames). Claude: if a required mock is missing, follow the UI mock
checkpoint in PLAN.md — prompt the author, don't invent the screen.
